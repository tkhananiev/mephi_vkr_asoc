# Отчёт о нагрузочном тестировании: ASOC / DefectDojo

**Дата:** 2026-05-08 (UTC).  
**Инструмент:** wrk 4.2.0 (`-t 8`, `-d 15s`, `--latency`, ступени `-c`).  
**ASOC:** Kubernetes, namespace `asoc`, сервис `api-service`, эндпоинт **`GET /health`**, доступ с рабочей машины через **`kubectl -n asoc port-forward svc/api-service 18080:8080`**.  
**DefectDojo:** **не выполнялось** (экземпляр недоступен для тех же параметров прогона).

---

## Результаты ASOC (`/health`, через kube port-forward)

| `-c` | Requests/sec | p50 latency | p99 latency |
|------|--------------|------------|-------------|
| 50   | 455.52       | 101.65 ms  | 310.96 ms   |
| 200  | 1384.97      | 112.85 ms  | 544.72 ms   |
| 400  | 2355.43      | 159.87 ms  | 1.21 s      |

HTTP-ошибок и аварийного завершения wrk в указанных прогонах не зафиксировано.

**Условность:** тракт включает накладные расходы **SSH/туннеля port-forward** и сеть между ноутбуком и кластером; это **не** «чистый RPS до Ingress».

---

## DefectDojo

Сравнительный прогон с тем же `-t` / `-d` и согласованным URL (**port-forward или тот же класс входа**) **не выполнялся**.

---

## Вывод на момент отчёта

- Для **`api-service`** в кластере на сценарии **`/health`** при росте соединений **200 → 400** наблюдается рост **p99 latency** до **порядка секунды** (см. таблицу).  
- Сопоставление с DefectDojo — **открытый пункт** следующего замера после выбора эквивалентного эндпоинта на стороне DD.

---

**Приложение:** полный вывод wrk — `loadtest/reports/wrk_k8s_api_health_20260508.txt`.
