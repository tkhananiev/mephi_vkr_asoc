# Статика + reverse proxy (см. web/nginx/default.conf). Образ: docker build -f web/Dockerfile -t asoc/web:latest .
---
apiVersion: v1
kind: Service
metadata:
  name: asoc-web
  namespace: asoc
spec:
  ports:
    - port: 80
      targetPort: 80
  selector:
    app: asoc-web
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: asoc-web
  namespace: asoc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: asoc-web
  template:
    metadata:
      labels:
        app: asoc-web
    spec:
      containers:
        - name: web
          image: asoc/web:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 2
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 10
            periodSeconds: 20
