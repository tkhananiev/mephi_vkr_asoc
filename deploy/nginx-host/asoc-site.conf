# Пример виртуального хоста: фронт (статика) + прокси API к docker-compose на localhost.
# Установка: см. deploy/nginx-host/README.md
#
# Подставь абсолютный путь к dist (после npm run build в mephi_vkr_asoc_front).

server {
    listen 80;
    server_name _;

    root /var/www/asoc/dist;
    index index.html;

    gzip on;
    gzip_types text/css application/javascript application/json image/svg+xml;

    # React Router (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Публичное API и Swagger (api-service)
    location /api/v1/scans {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        # Сценарий «Скан и паспорт»: Semgrep + processing + Jira дольше дефолтных ~60s nginx.
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
    location /openapi.yaml {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
    location /swagger {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
    location /health {
        proxy_pass http://127.0.0.1:8080;
        proxy_http_version 1.1;
    }
    # Агрегированные health для UI-мониторинга (каждый — /health целевого сервиса)
    location = /health/reference {
        proxy_pass http://127.0.0.1:8081/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
    location = /health/processing {
        proxy_pass http://127.0.0.1:8082/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
    location = /health/jira {
        proxy_pass http://127.0.0.1:8083/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
    location = /health/semgrep {
        proxy_pass http://127.0.0.1:8085/health;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    location /api/v1/sync {
        proxy_pass http://127.0.0.1:8081;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }

    location /api/v1/findings {
        proxy_pass http://127.0.0.1:8082;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
    location /api/v1/groups {
        proxy_pass http://127.0.0.1:8082;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
    location /api/v1/report {
        proxy_pass http://127.0.0.1:8082;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    location /api/v1/tickets {
        proxy_pass http://127.0.0.1:8083;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }
}
